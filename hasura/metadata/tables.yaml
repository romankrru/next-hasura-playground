- table:
    schema: public
    name: user
  insert_permissions:
  - role: public
    permission:
      check: {}
      columns:
      - firstName
      - lastName
      backend_only: false
  select_permissions:
  - role: public
    permission:
      columns:
      - firstName
      - lastName
      - createdAt
      - updatedAt
      - id
      filter: {}
